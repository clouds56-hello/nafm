Three-site fixture for repository integration tests.

Layout:
- `site_alpha`
- `site_beta`
- `site_gamma`

Properties:
- around 50 tracked files
- several duplicate families spanning 2-3 sites
- unique files mixed into each site
- multiple folders per site

This fixture is intended for higher-level scan, duplicates, missing, and stage tests.
